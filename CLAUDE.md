# Project Wiki

## Tool Preferences

- **Python package manager**: Use `uv` over `pip` for all Python operations (install, venv, run, etc.)
- **Python package manager**: graphify中的项目，也要在执行时使用uv python

## Documentation

- **Task steps**: 所有已完成的工作步骤记录在 `task_steps.md` 中，保持同步更新。
